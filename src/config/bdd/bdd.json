{
    "name": "PassManagerDataBase",
    "pragmaConfig": "PRAGMA journal_mode = WAL;",
    "initCommand": "CREATE TABLE IF NOT EXISTS Users (id INTEGER PRIMARY KEY NOT NULL, name TEXT NOT NULL, password TEXT NOT NULL, passwordfile TEXT NOT NULL);"
}